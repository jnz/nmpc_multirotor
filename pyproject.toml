[project]
name = "nmpc_multirotor"
version = "1.0.0"

dependencies = [
    "numpy>=1.21.2",
    "numpy-stl>=3.0.1",
    "scipy>=1.5.2",
    "pygame>=2.5.0",
    "PyOpenGL>=3.1.7",
    "casadi==3.7.0",
]

[tool.black]
line-length = 100

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
